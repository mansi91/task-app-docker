 Dockerized Full-Stack Task App 🐳

**Containerized web app** with **Nginx frontend**, **Python Flask backend**, and **PostgreSQL database**. Docker Compose orchestrates services with custom networks for secure communication and persistent volumes for data durability.

## Features ✨

- **Frontend**: Nginx serves responsive HTML + JavaScript
- **Backend**: Flask REST API (`/tasks` GET/POST endpoints)
- **Database**: PostgreSQL with persistent storage (`pgdata/`)
- **Networking**: Custom `app-net
